# --- Python & System ---
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# --- Virtual Environment ---
# We never commit the venv. Other devs create their own using requirements.txt
venv/
env/
.venv/

# --- Environment Variables (SECRETS) ---
# Never commit API keys or passwords!
.env
config/secrets.yaml

# --- IDE Settings ---
.vscode/
.idea/

# --- Project Data (Too large for Git) ---
# We ignore the files but keep the folders structure
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# --- Feast (Feature Store) ---
# These are local databases generated by 'feast apply'
# They should not be shared between computers.
data/feature_store/data/
.feast-test-server/

# --- Model Artifacts ---
# Models are binary blobs (large). 
# In production, these go to S3/Azure Blob Storage, not GitHub.
#models/production/*.joblib
models/staging/*.joblib

# --- Testing & Logs ---
.pytest_cache/
coverage.xml
htmlcov/
*.log